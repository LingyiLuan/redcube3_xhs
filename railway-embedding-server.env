# ============================================
# Embedding Server - Railway Environment Variables
# Paste this into Railway's "Raw Editor" for embedding-server
# ============================================
# 
# IMPORTANT: For this service:
# - Root Directory: Leave EMPTY or set to "."
# - RAILWAY_DOCKERFILE_PATH: Set to services/embedding-server/Dockerfile
# ============================================

# ============================================
# Railway Build Configuration (REQUIRED!)
# ============================================
# This tells Railway where to find the Dockerfile
# Set this in Railway Variables tab, NOT in Root Directory
RAILWAY_DOCKERFILE_PATH=services/embedding-server/Dockerfile

# ============================================
# Model Configuration
# ============================================
MODEL_NAME=BAAI/bge-small-en-v1.5

# ============================================
# Port Configuration
# ============================================
# Railway automatically sets PORT environment variable
# The service will use Railway's PORT (usually 8080)
# But the app.py uses PORT env var, so it should work automatically

# ============================================
# Environment
# ============================================
NODE_ENV=production

# ============================================
# Notess
# ============================================
# - Model will be downloaded on first request (takes 5-10 minutes)
# - Service will be available at: embedding-server-production-xxxx.up.railway.app
# - Update content-service EMBEDDING_SERVER_URL to point to this service
# - Root Directory in Railway Settings should be EMPTY or "."
